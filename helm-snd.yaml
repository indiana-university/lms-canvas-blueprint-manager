
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

activeProfiles: "redis,snd"

serviceAccountName: blueprint-manager-snd

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "blueprint-manager-snd.apps-snd.iu.edu"
      paths: []