
projectedSecret:
  - secret:
      name: "properties-2021-06-07"
  - secret:
      name: "lms-security-properties-20201014"

image:
  tag: stable

env:
  APP_ENV: prd

activeProfiles: "redis,prd"

serviceAccountName: blueprint-manager-prd

cluster: es-prod

ingress:
  enabled: true
  hosts:
    - host: "blueprint-manager.apps.iu.edu"
      paths: []