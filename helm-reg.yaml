
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

activeProfiles: "redis,reg"

serviceAccountName: blueprint-manager-reg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "blueprint-manager-reg.apps-test.iu.edu"
      paths: []
