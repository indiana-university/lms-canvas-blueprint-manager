
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

activeProfiles: "redis,stg"

serviceAccountName: blueprint-manager-stg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "blueprint-manager-stg.apps-test.iu.edu"
      paths: []